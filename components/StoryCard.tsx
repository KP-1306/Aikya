import Image from 'next/image';import Link from 'next/link';import { Story } from '@/lib/types';import { Play } from 'lucide-react';export default function StoryCard({ s }:{ s:Story }){return(<article className='card overflow-hidden'><Link href={`/story/${s.slug}`} className='block relative aspect-video'>{s.heroImage&&(<Image src={s.heroImage} alt={s.title} fill className='object-cover'/>) }{s.videoUrl&&(<div className='absolute top-2 left-2 badge bg-black/70 text-white'><Play className='w-3 h-3 mr-1'/> Watch</div>)}</Link><div className='p-4 space-y-2'><div className='flex items-center gap-2 text-xs text-neutral-500'><span className='badge'>{s.category}</span><span>• {s.city??s.state??s.country}</span><span>• {s.readMinutes} min read</span></div><h3 className='text-lg font-semibold leading-tight'><Link href={`/story/${s.slug}`}>{s.title}</Link></h3><p className='text-sm text-neutral-700'>{s.dek}</p><div className='pt-1 text-xs text-brand'>“{s.lifeLesson}”</div></div></article>);}